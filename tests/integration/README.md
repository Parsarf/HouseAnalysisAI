# Integration gates

Gate tests are added here by the lead agent as package worktrees merge. The gates are A (documents to units), B (units to facts), C (facts to scores), D (full stack), and E (ancillary capabilities), as defined in `acq-build-packages.md`.
