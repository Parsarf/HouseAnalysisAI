[importlinter]
root_packages = api, analyst, auth, calibration, changes, classification, common, contracts, db, exports, extraction, finance, flags, identity, ingestion, jobs, normalization, ops, pipeline, scoring, strategies

[importlinter:contracts]
name = Contracts are dependency-free
type = forbidden
source_modules = contracts
forbidden_modules = api, ingestion, classification, identity, extraction, normalization, finance, strategies, scoring, flags, pipeline

[importlinter:layers]
name = Layered package dependencies
type = layers
layers = contracts; common; ingestion, classification, identity, extraction, normalization, finance, strategies, scoring, flags, exports, changes, calibration, ops; pipeline; api
